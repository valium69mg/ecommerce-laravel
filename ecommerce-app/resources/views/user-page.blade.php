@include('header')
      <div class="hero_area">
         <!-- header section strats -->
         @include('navbar')
         <!-- end header section -->
         <!-- slider section -->
         @include('slider')
         <!-- end slider section -->
      </div>
      <!-- why section -->
      @include('why')
      <!-- end why section -->
      <!-- arrival section -->
      @include('arrival')
      <!-- end arrival section -->
      
      <!-- product section -->
      @include('product')
      <!-- end product section -->

      <!-- subscribe section -->
      @include('subscribe')
      <!-- end subscribe section -->
      <!-- client section -->
      @include('client')
      <!-- end client section -->
      <!-- footer start -->
      @include('info')
      <!-- footer end -->
      
@include('footer')